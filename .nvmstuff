#!/bin/bash

# if nvm isn't install, it should be...
if [ ! -f ~/.nvm/nvm.sh ]; then

  git clone https://github.com/nvm-sh/nvm.git ~/.nvm
  source ~/.nvm/nvm.sh
  nvm install 10.15.3
  nvm alias default 10.15.3
else
  source ~/.nvm/nvm.sh
fi
